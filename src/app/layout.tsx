import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SnapDL - Premium Video Downloader",
  description: "Download videos from YouTube, Instagram, and Facebook in high quality instantly.",
  keywords: "video downloader, youtube downloader, instagram reel download, facebook video download",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
